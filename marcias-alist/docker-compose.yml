version: "3.3"
services:
  app_proxy:
    environment:
      # The format here is: <app-id>_<docker-service-name>_1
      APP_HOST: marcias-alist_server_1
      APP_PORT: 5244
    
  server:
    image: 'xhofe/alist:latest'
    user: "1000:1000"
    init: true
