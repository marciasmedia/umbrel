version: "3.7"

services:
  node-red:
    image: nodered/node-red:latest
    environment:
      - TZ=Europe/Warsaw
    network_mode: host  
    ports:
      - "1880:1880"
    volumes:
      - ${APP_DATA_DIR}/data:/data
