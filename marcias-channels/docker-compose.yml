version: "3.7"
services:
  app_proxy:
    environment:
      # The format here is: <app-id>_<docker-service-name>_1
      APP_HOST: marcias-channels_server_1
      APP_PORT: 8089
    
  server:
    image: fancybits/channels-dvr:latest
    user: "1000:1000"
    init: true
